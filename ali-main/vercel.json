{
  "version": 2,
  "builds": [
    {
      "src": "app.py",  # Correct source file
      "use": "@vercel/python"  # Use Python for the build
    }
  ],
  "routes": [
    {
      "src": "/(.*)",  # Match all routes
      "dest": "/app.py"  # Direct all routes to app.py
    }
  ]
}
